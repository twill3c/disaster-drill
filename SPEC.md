# SPEC.md — disaster-drill(防災訓練シミュレーター)

<!-- scaffold template v1.30.0 から展開(2026-09-07)。以後このファイルはプロジェクトが育てる -->

元仕様書: 「防災訓練シミュレーター Webアプリ仕様書」v1.0.0(2026-09-07 受領)。
本ファイルはその仕様書を要求 ID に刻み直したもので、番号は仕様書の章番号ではない。
仕様書の章を引くときは「元仕様 §n」と書く。

## 1. 目的

災害発生後の状況をブラウザ上で疑似体験し、「この状況で自分なら何をするか」を選ぶことで
防災知識・判断力・優先順位づけを学ぶ Web アプリ。防災情報サイトではなく、
**災害発生 → 判断 → 結果 → 評価 → 再挑戦** というゲーム性を持つ「訓練シミュレーター」である
(元仕様 §1・§60)。

正解を選ぶクイズにはしない。「揺れている間は身を守る → 収まったら周囲を確認 → 避難判断」という
**状況依存の判断**を要求し(元仕様 §15)、選択がその後の状況(次イベント)を変える(元仕様 §12)。

**この訓練は教育目的である。** 実際の災害時は自治体・消防・警察・気象機関の最新情報と
現地の指示を優先し、ゲーム内の判断を一般化しない(元仕様 §56)。この注意書きは
アプリの画面に出す(F-17)。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 状態モデル: `SimulationState = { time, safety, health, supplies, information, evacuation, familySafety, score }`。`time` 以外は 0〜100 に**クランプ**する。`time` は経過秒で単調非減少(元仕様 §8) | must |
| F-02 | 行動モデル: `Action = { id, label, description, effects, explanation, riskLevel, next? }`。`effects` は各状態の増分(省略は 0)。`riskLevel` は `low / medium / high`。`next` は分岐先イベント ID(省略時はイベント側の `nextEvent`)(元仕様 §9・§12) | must |
| F-03 | シナリオモデル: `Scenario = { id, title, disasterType, difficulty(1..5), introduction, initialState, events[] }`、`ScenarioEvent = { id, title, description, actions[], nextEvent?, timeLimit?, info?, tip? }`。**プログラム本体とシナリオデータを分離**する(`src/data/*.ts`)(元仕様 §10) | must |
| F-04 | 遷移: `applyAction(state, action)` は純関数。効果を加算しクランプし、`history` に `{ eventId, actionId, at: time }` を積む。次イベントは `action.next ?? event.nextEvent`。どちらも無ければ終了(元仕様 §6・§12) | must |
| F-05 | スコア: `score = safety×0.30 + evacuation×0.25 + health×0.15 + information×0.10 + timeScore×0.10 + familySafety×0.10`。**`timeScore` は「経過時間の少なさ」を 0〜100 に写した値**で、シナリオの `timeBudget`(秒)に対し `100 × max(0, 1 − time/timeBudget)` とする(元仕様 §13 は「時間 ×0.10」としか書かないので、ここで定義を補った。実測後に較正する) | must |
| F-06 | ランク: 90〜100 S / 80〜89 A / 70〜79 B / 60〜69 C / 0〜59 D。境界は**切り捨て後の整数**で判定(元仕様 §14) | must |
| F-07 | 地震シナリオ EQ01: 自宅リビングで揺れ → 揺れ収束 → 周囲確認/ガス/避難判断 → 避難所到着の分岐つき 5 イベント以上。「外へ飛び出す」は安全度を大きく下げ、別イベントへ分岐する(元仕様 §11) | must |
| F-08 | 訓練画面: 見出し(災害名・経過時間)/ SVG マップ / 状態パネル / 行動ボタン列。**縦積み**で、スマートフォン幅でも横スクロールしない(元仕様 §7・§37) | must |
| F-09 | 結果画面: 総合点・ランク・各指標・良かった判断/改善ポイント・行動履歴タイムライン・「もし別の行動をしていたら」の比較(各分岐点で選ばなかった行動を 1 ステップだけ適用した安全度)・再挑戦(元仕様 §22〜§24) | must |
| F-10 | 保存: localStorage キー `disaster-drill-history` に最高スコア・クリア回数・災害別最高ランク・プレイ履歴(最新 20 件)・設定。**個人情報は保存しない**。壊れた値はパース失敗として初期値扱い。localStorage 不可でも訓練は続行できる(元仕様 §27・§46) | must |
| F-11 | 訓練モード: Beginner(時間制限なし・解説を毎回表示)/ Standard(時間制限あり・解説は最後)/ Challenge(時間制限あり・情報量制限・ランダムイベント)(元仕様 §25) | should |
| F-12 | タイムリミット: `timeLimit` を持つイベントは残り秒を表示し、0 で「判断が間に合わなかった」として `timeoutAction`(無ければ最もリスクの高い行動)を適用する。Beginner では無効(元仕様 §16) | should |
| F-13 | 防災知識カード: イベントごとに `tip` を持ち、Beginner では行動選択直後に、他モードでは結果画面にまとめて表示(元仕様 §21) | must |
| F-14 | アニメーション: CSS / SVG のみで災害種別ごとの表現(揺れ・炎・水位上昇・波・土砂)。外部ライブラリ不使用。`prefers-reduced-motion` で停止(元仕様 §17・§36) | should |
| F-15 | 危険度表示: `danger = 100 − safety` を LOW(<34)/ MEDIUM(<67)/ HIGH の三段で、**色と文字の両方**で表示(元仕様 §19・§36) | must |
| F-16 | 5 災害: EQ01 地震 / FI01 火災 / FL01 洪水 / TS01 津波 / LS01 土砂災害。シナリオ一覧から選べる(元仕様 §5) | must |
| F-17 | 免責: トップと結果画面に「教育目的・実際の災害では公的情報と現地の指示を優先」を表示(元仕様 §56) | must |
| F-18 | ランダムイベント: Challenge では `random.ts` のシード付き PRNG で、イベントの `variants` から一つを選ぶ。シードは画面に出し、指定して再挑戦できる(元仕様 §26) | should |
| F-19 | 自己ベスト: 災害別の最高点とランクの一覧(元仕様 §28) | should |
| F-20 | 開発ハーネス: `npm run verify`(typecheck + lint + test + build)green が全ループの完了条件 | must |
| F-21 | フッタ: フリート統一書式(「MIT License © 2026 坂田哲朗 ・ GitHub ・ 歩き方 ・ 設計図 ・ App Menu」・`position: fixed; bottom: 0`・区切りの「・」は文字として置く) | must |
| F-22 | チュートリアル: 初回起動時のみ 5 ステップの手順を表示。既読は F-10 の設定に保存(元仕様 §40) | should |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | `src/core/` は純関数のみ。`Math.random()` / `Date.now()` / `new Date()` / DOM 大域変数をコードとして書かない。乱数は `random.ts` の PRNG をシード注入で使う | T-090(字句走査 + 陽性対照) |
| N-02 | Next.js 静的エクスポート(`output: "export"`)。ランタイム依存は react / react-dom / next のみ。バックエンド・DB・外部 API・環境変数・認証なし(元仕様 §30・§43) | T-091(`package.json` の dependencies 鍵集合と `next.config.ts` の設定を直接見る) |
| N-03 | 全シナリオは**到達可能**である: 開始イベントから遷移を辿ってすべてのイベントに到達でき、参照先 ID は必ず存在し、循環は持たず、すべての経路が有限ステップで終端に至る | T-030〜T-034(データ検査) |
| N-04 | 全シナリオは**判断が効く**: どのイベントでも行動によって `safety` の変化量が同一でない(全部同じなら「判断」ではない) | T-035 |
| N-05 | アクセシビリティ: 行動はすべて `<button>`、状態値は数値テキストを併記、`aria-live` で状況更新を通知、`prefers-reduced-motion` 対応 | T-095(静的 HTML 走査)+ 実ブラウザ検品 |
| N-06 | 日本語本文に別字種・制御文字を混入させない | `python harness/text_hygiene.py` |

## 4. 品質基準

| ゲート | 内容 | 検査 |
|---|---|---|
| G-01 | スコアの重みの和が 1.000 であり、全指標 100 のとき 100 点、全指標 0 のとき 0 点 | T-010 |
| G-02 | ランク境界: 89→A, 90→S, 79→B, 80→A, 69→C, 70→B, 59→D, 60→C(元仕様 §14 の表から) | T-011 |
| G-03 | クランプ: どんな効果を何回重ねても状態値は [0,100] を出ない(ランダム 1,000 回の性質テスト、PRNG シード固定) | T-020 |
| G-04 | 全シナリオの到達可能性・参照整合・非循環・終端保証(N-03) | T-030〜T-034 |
| G-05 | 全シナリオ・全イベントで行動間に `safety` 効果の差がある(N-04) | T-035 |
| G-06 | 保存値の往復: 保存 → 読込で同値。壊れた JSON・型違いは初期値に落ちる | T-040〜T-042 |
| G-07 | 純粋性(N-01) — 検査器の陽性対照 6 種・陰性対照 5 種つき(実測 2026-09-07) | T-090 |
| G-08 | 出荷構成(N-02) | T-091 |
| G-09 | `next build` が通り `out/` に全ルートの HTML がある | `npm run verify` + T-092 |
| G-10 | 静的 HTML にフッタ 5 項目が並び順どおり在る(F-21) | T-093 |
| G-11 | 免責文がトップと結果画面に在る(F-17) | T-094 |
| G-12 | 比較機能(F-09)の値は `applyAction` と同じ関数で出す(表示用の別計算を持たない) | T-050 |
| G-13 | PRNG は同一シードで同一列、異なるシードで異なる列(F-18) | T-060 |
| G-14 | 実ブラウザ(1280 / 390 の 2 幅)でコンソールのエラー 0・横溢れ 0・図が `viewBox` に収まる。**図と棒を実際に掴めていること**も同時に測る(掴めていなければ落とす) | `scripts/browser_check.py` |
| G-15 | 文書中の数値が実装と一致する(README のシナリオ表) | T-080 |
| G-16 | SPEC の各 `G-xx` が TEST_SPEC から参照される | T-081 |

### 実ブラウザ検品(G-14)

**目で見なければ分からない性質は、テストで代替せず検品の手順として残す。**
図を触ったループ、画面の見た目を変えたループは `python scripts/browser_check.py` を
完了条件に含める。loop_001 では、単体 113 件・画面 16 件・出荷物 14 件がすべて緑のまま、
**状態パネルの帯が棒に見えない**欠陥が目視でだけ見つかった(`display` を指定しない
`span` に `height` は効かない)。検品器にその検出を足したが、**足した検出が二度空振りした** ——
一度目は帯の出る画面まで進んでいなかったため、二度目は閾値を測らずに置いたため。
詳細は HC-080 の追加証拠。

### 目玉と、それをどう測るか

目玉は「判断が次の状況を変える」ことである。これは N-03/N-04 の**データ検査**で守る。
「正解を選ぶだけ」に退化していないかは、**各シナリオに分岐(next の異なる行動)が最低 1 箇所ある**
ことを T-036 で数える。分岐 0 のシナリオは出荷しない。

## 5. スコープ外

- マルチプレイヤー・AI 生成・シナリオエディタ・地域防災モード(元仕様 §52〜§55)
- サウンド(元仕様 §41)
- PWA / Service Worker(元仕様 §29 は「可能な構造」まで)
- オンラインランキング(元仕様 §28)
- 台風・大雪・落雷ほかの追加災害(元仕様 §5 の将来分)
