# dependencies / build
node_modules/
.next/
out/
coverage/
*.tsbuildinfo
next-env.d.ts

# env / deploy
.env*
.vercel/

# loop observability(verify の実行ログは append-only・gitignore)
.loop/

# worktree baseline(worktree ごとのローカル状態)
.wt-baseline.json

.vercel

# 検品のスクリーンショット(手で見るためのもの。追跡しない)
shots/

# Python
__pycache__/
*.pyc
