# TEST_SPEC.md — disaster-drill

<!-- scaffold template v1.30.0 から展開(2026-09-07) -->

## 実行規約

- `npx vitest run`(`tests/*.test.ts`)を stage 3–5 の判定に使用する
- `tests/validation/*.test.ts` は **`next build` の後に** `npm run verify` から走る(`out/` を検査するため)
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提を**テスト内の assert で検算**し、
  導出過程をコメントに残す(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。SPEC の保証粒度を超える期待値を書かない |
| 外部権威 | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | 実測日と実測値をコメントに残す |

件数・行数は定数で書かず、集合の一致・取りこぼしの不在という不変量で書く。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| 重み・ランク境界 | 元仕様 §13・§14(SPEC F-05・F-06) | 仕様の定数 |
| 全シナリオデータ `src/data/*.ts` | 自作 | データ検査の対象。到達可能性・非循環はグラフ探索で機械的に判定 |
| クランプの性質テスト | `random.ts` の PRNG(seed 固定)で効果を 1,000 回生成 | 性質(不変量)。定数を持たない |
| 純粋性検査の陽性/陰性対照 | テスト内の文字列 | 検査器自体を検査する(HC-041) |

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-010 | F-05 / G-01 | 重みの和・全 100・全 0 | 和 = 1(誤差 1e-9)。全指標 100・time 0 → 100 点。全指標 0・time ≥ budget → 0 点 |
| T-011 | F-06 / G-02 | ランク境界 8 点 | 89→A, 90→S, 79→B, 80→A, 69→C, 70→B, 59→D, 60→C |
| T-012 | F-05 | timeScore | time 0 → 100、time = budget → 0、time > budget → 0、time = budget/2 → 50 |
| T-013 | F-09 | evaluate | riskLevel low の行動が「良かった判断」に、high が「改善ポイント」に入る |
| T-020 | F-01 / G-03 | クランプの性質 | PRNG(seed 20260907)で ±200 の効果を 1,000 回重ねても各指標 ∈ [0,100]、time は非減少 |
| T-021 | F-04 | applyAction | 効果が加算され、history に `{eventId, actionId, before, at}` が積まれ、before は行動前の状態と同値 |
| T-022 | F-04 | 遷移先の決定 | `action.next` があればそれ、無ければ `event.nextEvent`、どちらも無ければ `eventId === null` |
| T-023 | F-12 | timeoutAction | `timeoutActionId` があればそれ、無ければ riskLevel high の最初の行動 |
| T-024 | F-15 | dangerLevel | safety 100→LOW, 67→LOW, 66→MEDIUM, 34→MEDIUM, 33→HIGH, 0→HIGH |
| T-025 | F-18 | variant の適用 | `enterEvent` で PRNG が選んだ variant の効果が state に乗り、history に variantId が残る。Beginner/Standard では乗らない |
| T-030 | N-03 / G-04 | 参照整合 | 全シナリオで `next` / `nextEvent` / `timeoutActionId` の参照先が存在する |
| T-031 | N-03 / G-04 | 到達可能性 | 開始イベントから全イベントへ到達できる(到達不能集合が空) |
| T-032 | N-03 / G-04 | 非循環 | イベントグラフに閉路がない |
| T-033 | N-03 / G-04 | 終端保証 | 全経路が有限ステップで `null` に至る(非循環 + 各イベントに行動 ≥ 2) |
| T-034 | F-03 | ID の一意性 | シナリオ ID・イベント ID・イベント内の行動 ID が重複しない。シナリオ数は F-16 の集合 {EQ01,FI01,FL01,TS01,LS01} と一致 |
| T-035 | N-04 / G-05 | 判断が効く | 全イベントで行動間の `safety` 効果が全て同じではない |
| T-036 | §4 目玉 | 分岐の存在 | 各シナリオに、同一イベント内で遷移先が異なる行動の組が最低 1 つある |
| T-037 | F-13 | 解説の存在 | 全行動に `explanation` が空でない。全イベントに `tip` か `info` のいずれかがある |
| T-040 | F-10 / G-06 | 往復 | recordResult → save → load で同値 |
| T-041 | F-10 / G-06 | 壊れた値 | 不正 JSON・型違い・null は初期値 |
| T-042 | F-10 / G-06 | 集計 | 最高点は max、クリア回数は +1、災害別ランクは最良、履歴は最新 20 件 |
| T-043 | F-10 | storage 不可 | getItem が例外を投げても初期値を返し、setItem の例外は false を返す |
| T-050 | F-09 / G-12 | 比較 | `alternatives` の各値は `applyEffects(entry.before, other.effects).safety` と一致(同じ関数で出す) |
| T-060 | F-18 / G-13 | PRNG | 同一シード同一列(100 個)、異なるシードで列が異なる、値 ∈ [0,1)、`int(n)` ∈ [0,n) |
| T-090 | N-01 / G-07 | 純粋性 | `src/core/*.ts` に `Math.random` / `Date.now` / `new Date` / `window` / `document` / `localStorage` が無い。陽性対照 6 種を検査器が捕まえ、陰性対照(コメント内の言及・`window` を含む識別子)は通す。走査対象が空でない |
| T-091 | N-02 / G-08 | 出荷構成 | dependencies の鍵集合 = {next, react, react-dom}。`next.config.ts` に非コメント行として `output: "export"` がある |
| T-092 | G-09 | ビルド成果 | `out/<ルート>.html` が全ルート分ある。**経路は実測から取った**(2026-09-07 に `ls out/`。`trailingSlash` 既定なので `out/scenarios.html` であって `out/scenarios/index.html` ではない — HC-196)。走査対象が空でないことも見る(validation) |
| T-093 | F-21 / G-10 | フッタ | 全 HTML にフッタ 5 項目 + © がこの順で在る。**共通部分**(MIT License / GitHub / App Menu / ©)はリテラルで固定し、**アプリ固有の 3・4 番目は `FOOTER_ITEMS` から導く** —— 他アプリの文言を写さないため(HC-196)。区切りの「・」が文字として 4 つ在ることも数える(validation) |
| T-094 | F-17 / G-11 | 免責 | `out/index.html` と `out/result.html` に「教育」「現地の指示」を含む文がある。**免責はサーバ側の外枠に置く** —— クライアント状態の内側に置くと事前描画された HTML に入らない(validation) |
| T-095 | N-05 | a11y 静的走査 | 事前描画される殻を測る: 全ページに `lang="ja"`、一覧の図に代替テキスト。**訓練画面の a11y はここでは測れない**(クエリ依存でSuspense より内側が事前描画されない)ので T-100 が受け持つ |

### 受け渡しと文書(loop_001 で追加)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-070 | F-09 | 訓練 → 結果の受け渡し | 保存して読み戻せる。`window` 不在・壊れた値・例外で null / false を返し落ちない |
| T-071 | F-21 / G-10 | フッタ規約 | 5 項目・並び・共通文言(MIT License / GitHub / App Menu / ©)・全項目に https の遷移先 |
| T-080 | F-03 / F-16 / G-15 | README の数値の検算 | README のシナリオ表の ID 集合・場面数・難易度が実装と一致する。表が空でないことと、数を変えた行が落ちることも見る(HC-152) |
| T-081 | 品質ゲート / G-16 | SPEC と実装の対応 | SPEC の各 `G-xx` が TEST_SPEC から参照されるか「未実装」と明記されている。TEST_SPEC の各 `T-xxx` がテストファイルに現れる(HC-157) |

### 画面の検査(jsdom で実際に描画する。元仕様 §45 の E2E に相当)

**在存ではなく振る舞いで書く**(HC-080): 読み上げ名で掴み、実際に押し、状態の変化を見る。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-100 | F-08 / N-05 | 訓練画面の構造 | 開始前から `aria-live` の器が在る。行動はすべて `button` で読み上げ名から掴める。危険度が `meter` と文字の両方で出る。live 領域に現在の場面が入る |
| T-101 | F-04 / F-08 | 行動で状態が変わる | 安全な行動で安全度が上がり解説が出る。危険な行動で下がる。`next` を持つ行動は分岐先へ、持たない行動は既定の次へ進む |
| T-102 | F-11 | モードの違い | 標準では解説をはさまず次の場面へ進み、残り時間が出る。初心者では残り時間が出ない |
| T-103 | F-12 | 時間切れ | 残り時間が 0 になると自動で行動が選ばれ、`role="alert"` でその旨が出る。タイマー送りは `act` で包む |
| T-104 | F-09 / F-10 | 終了 | 終端で走行を保存し `/result` へ遷移する。30 手で終わらなければ落とす(遷移の循環を捕まえる) |
| T-105 | F-09 / F-10 | 結果画面 | 評価・履歴・比較・防災ポイントが出て、成績が localStorage に保存される。記録が無ければその旨を出す |
| T-106 | 元仕様 §46 | 未知のシナリオ | 読み込めない旨とトップへの導線を出す |
| T-107 | F-22 | チュートリアル | 初回は 5 手順が出る。閉じると消えて既読が保存される。既読なら出ない。**陽性対照**: 既読の印を消せば再び出る |

### 実ブラウザ検品(`python scripts/browser_check.py`)

自動テストで代替できない性質を、実際の Chromium で 2 つの画面幅(1280 / 390)から測る。

`G-14` を確かめる工程である。**テストではなく手順**なので、`npm run verify` からは呼ばない ——
実ブラウザの起動を含むため、図・画面を触ったループの完了条件として手で走らせる。

| 測るもの | 落とす条件 |
|---|---|
| コンソールのエラー・失敗した要求 | 1 件でもあれば |
| 横溢れ | `scrollWidth > clientWidth`(自前で横スクロールする入れ物の中身は除く) |
| 図の収まり | `getBBox()` が `viewBox` を出る要素があれば |
| **図を実際に見ているか** | 図が在るべき画面(`scenarios` / `play`)で `svg` を 1 枚も掴めなければ |
| **棒が潰れていないか** | `.stat__bar` 等の実測の高さが 3px 未満なら。**インライン要素に `height` は効かない**という故障を捕まえる(loop_001) |
